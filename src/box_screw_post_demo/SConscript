#!python
import itertools

Import('env openscad2')
printable_parts = ['top', 'bottom']
all_parts = ['demo'] + printable_parts

for part in all_parts:
  openscad2(target='box_screw_post_demo_' + part,
            source=['box_screw_post_demo'], path="src/box_screw_post_demo")

env.Alias('box_screw_post_demo',
          ['box_screw_post_demo_' + part + suffix
           for part in all_parts
           for suffix in ['.png', '.stl']])

env.Alias('box_screw_post_demo_printable_parts',
          ['box_screw_post_demo_' + part + '.stl'
           for part in printable_parts])
